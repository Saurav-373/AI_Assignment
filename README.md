# AI Assignment - Resume Screening AI

This project trains a resume-category classifier and provides a CLI tool to screen resumes using:
- ML prediction (TF-IDF + Logistic Regression)
- ATS-style skill matching for job roles

- `src/` : all Python scripts
- `report/` : report files
- `tools/` : extra utilities
